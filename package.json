{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "16.x"
  },
  "directories": {
    "lib": "./backend/lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install --prefix ./frontend && npm run build --prefix ./frontend",
    "rails": "bundle install --prefix ./backend && rails db:migrate db:seed --prefix ./backend",
    "server": "rails s --prefix ./backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
